#!/usr/bin/env bash
set -euo pipefail

# ── stage-7-nfr-bench.sh ─────────────────────────────────────────────────────
# Fires: After build verification tool calls in Stage 7
# Enforces: Non-functional requirement benchmarks are captured — binary
#   size, launch time, memory footprint. Results are compared against
#   baseline thresholds defined in the PRD.
# ──────────────────────────────────────────────────────────────────────────────

echo "STUB: stage-7-nfr-bench — implement before Stage 7 runs"
exit 0
