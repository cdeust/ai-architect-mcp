#!/usr/bin/env bash
set -euo pipefail

# ── stage-6-double-policy.sh ─────────────────────────────────────────────────
# Fires: After every file-write tool call in Stage 6
# Enforces: No file may define more than one public class or type.
#   The one-type-per-file rule prevents god objects and ensures each file
#   has a single responsibility.
# ──────────────────────────────────────────────────────────────────────────────

echo "STUB: stage-6-double-policy — implement before Stage 6 runs"
exit 0
