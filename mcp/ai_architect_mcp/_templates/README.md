# Templates

This directory contains output templates used by the MCP server tools.

Templates define the structure of generated artifacts (PRDs, verification
reports, audit trails) without containing any logic. The MCP tools
populate these templates with computed data.

Templates are referenced by tool code — they are not skills and are not
read by Claude directly.
